apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: shopflow
spec:
  selector:
    app: nginx
  ports:
    - port: 80
      targetPort: 80
  type: NodePort   # для доступа извне minikube